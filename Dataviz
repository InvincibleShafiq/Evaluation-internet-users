import pandas as pd 
import numpy as np
import matplotlib.pyplot as plt 
import datetime
import random
import seaborn as sns 
import os
df=pd.read_csv(r'gapminder_internet.csv')
df.set_index('country')

df.set_index('country')['Finland' :'Germany']

dfs=df.set_index('country')['Finland' :'Germany']

dfs.head()
dfs.plot(kind='bar',figsize=(12,5))
plt.title('Evolution internetuserate')
